$(document).ready(function(){
  $(".dropdown-trigger").dropdown({
    hover: true,
    constrainWidth: false,
    inDuration: 500,
    outDuration: 400,
  });
})


$(document).ready(function(){
  $('.sidenav').sidenav({
    closeOnClick: true,
    edge: 'right',
  });
});
